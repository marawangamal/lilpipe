evaluations:
  wmdp-cyber-and-mmlu:
    script: scripts/lilpipe/eval_wmdp.sbatch
    args:
      - "{model.id}"
      - "{model.model_name_or_path}"
      - cyber
    sbatch_args:
      - --gres=gpu:l40s:1
      - --cpus-per-task=8
      - --mem=64G
      - --time=02:00:00
